import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter'
})

export const metadata: Metadata = {
  title: 'Native Lens Photography | Senior, Athlete, Family & Branding Photography',
  description: 'Native Lens Photography creates intentional, original, and purposeful photography for seniors, athletes, families, and brands in Colorado.',
  icons: {
    icon: '/native-lens-logo.png',
    apple: '/native-lens-logo.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
